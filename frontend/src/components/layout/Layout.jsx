// Layout: thin wrapper preserving the historical import path. Shell lives in shell/AppShell.
import AppShell from '../../shell/AppShell';

export default function Layout() {
  return <AppShell />;
}
